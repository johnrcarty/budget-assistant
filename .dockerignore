node_modules
.next
.git
.env*
!.env.example
*.log
npm-debug.log*

# contains real household financial data - gitignored, never bake into an
# image either (see .gitignore's matching entry)
scripts/seed.ts

# real household DB dumps - never bake into an image
/backups/
