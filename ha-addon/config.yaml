name: Budget Assistant
slug: budget_assistant
version: "0.1.0"
description: Self-hosted zero-based budgeting and finance tracker
arch:
  - amd64
startup: application
boot: auto
init: false
ingress: true
# nginx (rootfs/etc/nginx/http.d/ingress.conf) rewrites root-absolute asset
# references using the X-Ingress-Path header before they reach the browser -
# the Next.js app itself now only listens on an internal loopback port.
ingress_port: 8099
panel_icon: mdi:cash-multiple
panel_title: Budget Assistant
options:
  household_login_email: ""
  household_login_password: ""
  auth_secret: ""
  simplefin_encryption_key: ""
  anthropic_api_key: ""
  mcp_auth_token: ""
  backup_retention_days: 14
schema:
  household_login_email: email
  household_login_password: password
  auth_secret: password
  simplefin_encryption_key: password
  anthropic_api_key: "str?"
  mcp_auth_token: "str?"
  backup_retention_days: int
